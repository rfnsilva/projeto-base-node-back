projeto base para criar aplicação com Node, typescript, babel, typeorm, jest, eslint

## Getting Started

```bash
yarn install para instalar as dependencias
yarn dev para executar
yarn build para gerar build de produção
```
